# Flight Reservation System Web UI - Task List

- `[x]` **Phase 1: Project Setup (Backend Infrastructure)**
    - `[x]` Create `pom.xml` for dependency management (Maven)
    - `[x]` Restructure project to standard Maven directory (`src/main/java/`)
    - `[x]` Setup Java REST API using **Javalin**
    - `[x]` Create flight discovery endpoint (e.g., `GET /api/flights`)
- `[ ]` **Phase 2: Database Bridge**
    - `[ ]` Adapt `dbManager.java` for the API
    - `[ ]` Create JSON-friendly model transformations
- `[ ]` **Phase 3: Frontend Initialization**
    - `[ ]` Setup Vite + React in `frontend/` directory
    - `[ ]` Build the Flight Search landing page (Aesthetic & Dynamic)
- `[ ]` **Phase 4: Feature Integration**
    - `[ ]` Implement User Login (Integrated with current Java logic)
    - `[ ]` Implement Seat Selection & Booking UX
    - `[ ]` Build the Admin/Flight Agent dashboards
